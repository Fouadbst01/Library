{% extends 'base.html.twig' %}

{% block title %}Genre index{% endblock %}

{% block body %}
    {# <h1>Genre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for genre in genres %}
            <tr>
                <td>{{ genre.id }}</td>
                <td>{{ genre.nom }}</td>
                <td>
                    <a href="{{ path('genre_show', {'id': genre.id}) }}">show</a>
                    <a href="{{ path('genre_edit', {'id': genre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('genre_new') }}">Create new</a> #}
    <section class="snew">
	    <div class="new-container">
            {% for key,genre in genres %}
            <div class="box-genre">
                <img src="../images/g{{key%4+1}}.jpg" />
                <div class="greenshadow"></div>
                <div class="box-gtitle">
                    <h3>{{ genre.nom }}</h3>
                </div>
            </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}
